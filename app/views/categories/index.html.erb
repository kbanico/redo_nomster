<h1 class="text-center">Listing All Categories</h1>
<% @categories.each do |category| %>
  <%= link_to category.name, category_path(category) %>
  <small><%= pluralize(category.places.count,"Place")%></small>
  <br>
<% end %>
<%= will_paginate %>
