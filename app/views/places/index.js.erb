$('#places').append("<%= escape_javascript(render 'place')%>");
$('#paginator').html("<%= escape_javascript(link_to_next_page(@places, 'LOAD MORE', remote: true, id: 'load_more'))%>");
if (!$('#load_more').length) { $('#paginator').remove(); }
