<% @places.each do |place| %>
  <div class="booyah-box col-10 offset-1">
    <h1><%= place.name %></h1>
    <i><%= place.address %></i>
    <p><%= place.description %></p> <br><br>
  </div>

<% end %>

<div class="text-center">
  <%= will_paginate @places %>
</div>
